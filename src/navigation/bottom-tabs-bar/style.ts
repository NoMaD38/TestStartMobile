import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  tabBar__label: {
    fontFamily: 'Raleway-Medium',
    fontSize: 10,
    lineHeight: 12,
  },

  tabBar__style: {
    height: 40,
  },
});
